# yaml-language-server: $schema=https://json-schema.org/draft/2019-09/schema
description: The set of principals authorised to access the operator UI.
type: object
required: [principals]
properties:
  principals:
    description: All authorised operator principals.
    type: array
    items:
      $ref: './OperatorPrincipalDetail.yaml'
